#include<bits/stdc++.h>
using namespace std;
#define int long long
#define Mod 1000000007
int Solve(vector<int>&arr, int n,int m)
{
	
}
signed main()
{
	int n,m;
	cin>>n>>m;
	vector<int> arr(n);
	for(int i=0; i<n; ++i)
		cin>>arr[i];
	cout<<Solve(arr,n,m);
}